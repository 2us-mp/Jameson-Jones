# Jameson-Jones

This repository now includes a minimal real-mode boot workflow for a **32-bit x86 kernel** that boots through GRUB/Multiboot in virtual machines.

See `os32/README.md` for the full roadmap, source layout, and build/run instructions.
